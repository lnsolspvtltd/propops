# API base URL (used by Next.js server components and API routes)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Temporary default org ID until auth is implemented in Phase 3
# Run POST /api/v1/onboarding/setup once and paste the org_id here
NEXT_PUBLIC_DEFAULT_ORG_ID=
